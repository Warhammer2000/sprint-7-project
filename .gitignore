# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# Secrets / local config
.env
*.key

# Векторный индекс по умолчанию коммитим (tfidf, небольшой) — бот запускается
# «из коробки» без пересборки. Пересобрать: python build_index.py
# (st-индекс с torch не коммитим — он тяжелее и тянет модель при запросе)

# Logs (keep the folder, ignore contents except samples)
logs/*.jsonl
logs/*.log
!logs/.gitkeep
!logs/sample_*.jsonl
!logs/sample_*.log

# OS
.DS_Store
Thumbs.db

# Models cache (sentence-transformers / HF)
.cache/
models/
